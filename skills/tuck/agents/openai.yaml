interface:
  display_name: "Tuck"
  short_description: "Commit with batched subagent review"
  default_prompt: "Use tuck to create focused, reviewed commits for my local changes."

policy:
  allow_implicit_invocation: true
