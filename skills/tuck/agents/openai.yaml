interface:
  display_name: "Tuck"
  short_description: "Create local commits with batched review"
  default_prompt: "Use tuck to create focused, reviewed local commits for my changes."

policy:
  allow_implicit_invocation: true
