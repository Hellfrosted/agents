interface:
  display_name: "ICM Memory"
  short_description: "Recall and store ICM memory for a task"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  brand_color: "#4338CA"
  default_prompt: "Use $icm to recall relevant ICM memory and store only explicit safe durable facts when appropriate."

policy:
  allow_implicit_invocation: true
