interface:
  display_name: "Confidence Loop"
  short_description: "Stress-test strategies and Codex loops with second opinions"
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
  brand_color: "#1D4ED8"
  default_prompt: "Use $confidence-loop c to audit this strategy or Codex loop until every real loophole has a concrete fix. Use `a` for hard mode or `s` for extreme mode."

policy:
  allow_implicit_invocation: true
